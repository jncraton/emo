Simple HTML app with single, large find-as-you type emoji picker.

When the first return emoji is automatically copied to the clipboard.

The current search term is loaded from the URL hash if it is present. The URL has is updated to match the current search term as the user types.